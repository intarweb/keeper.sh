import { unimplemented } from "../unimplemented";

const verifyChannelToken = (presented: string, storedHash: string): boolean => unimplemented(presented, storedHash);

export { verifyChannelToken };
